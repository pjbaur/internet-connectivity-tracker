spring:
  application:
    name: internet-connectivity-tracker

ict:
  probe:
    interval-ms: 1000       # 1 second default
  elasticsearch:
    host: elasticsearch
    port: 9200
    scheme: http
    index: probe-results
